# Automation

This directory is reserved for scripts and helpers that extract benchmark metrics, generate summaries, and prepare structured outputs for downstream use.

Current subdirectories:

- `scripts/`: executable automation helpers
- `templates/`: report templates, schemas, and structured output definitions
