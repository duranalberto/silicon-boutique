# Infrastructure

This directory contains Infrastructure as Code used by SiliconBoutique.

Current subdirectories:

- `terraform/`: Terraform definitions for local Kubernetes validation, guarded GCP and AWS benchmark paths, and durable BigQuery summary storage
